import { NoteEditor } from "../notes";
import { getBuiltinNotetypes } from "@/builtin/notetypes";

export interface InitCoreOptions { }

export async function initCore(options: InitCoreOptions = {}) {
  console.log("[core] initCore");
}
